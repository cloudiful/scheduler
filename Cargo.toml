[package]
name = "scheduler"
version = "0.2.0"
edition = "2024"

[dependencies]
chrono = {version = "0.4.41", features = ["serde"]}
chrono-tz = "0.10.4"
tokio = {version = "1.47.1", features = ["full"]}
